import { Toggle } from "@/components";
import { ComponentPreview, PropsTable, Callout, ToggleBasicDemo, ToggleOnDemo, ToggleColorsDemo, ToggleSizesDemo, ToggleDisabledDemo } from "@/components/docs";

# Toggle

Sliding ON/OFF switch with glowing indicator and monospaced status text. Uses `role="switch"` for accessibility. Requires controlled state via `checked` and `onChange`.

```tsx
import { Toggle } from "@mattloyed/eva-ui";
```

## Off State

<ComponentPreview
  title="OFF"
  code={`const [checked, setChecked] = useState(false);

<Toggle checked={checked} onChange={setChecked} label="SYSTEM ACTIVE" />`}
>
  <ToggleBasicDemo />
</ComponentPreview>

## On State

<ComponentPreview
  title="ON"
  code={`const [checked, setChecked] = useState(true);

<Toggle checked={checked} onChange={setChecked} label="NEURAL LINK" />`}
>
  <ToggleOnDemo />
</ComponentPreview>

## Colors

<ComponentPreview
  title="COLORS"
  code={`<Toggle checked={true} onChange={setChecked} color="orange" label="ORANGE" />
<Toggle checked={true} onChange={setChecked} color="green" label="GREEN" />
<Toggle checked={true} onChange={setChecked} color="cyan" label="CYAN" />`}
>
  <ToggleColorsDemo />
</ComponentPreview>

## Sizes

<ComponentPreview
  title="SIZES"
  code={`<Toggle checked={true} onChange={setChecked} size="sm" label="SMALL" />
<Toggle checked={true} onChange={setChecked} size="md" label="MEDIUM" />
<Toggle checked={true} onChange={setChecked} size="lg" label="LARGE" />`}
>
  <ToggleSizesDemo />
</ComponentPreview>

## Disabled

<ComponentPreview
  title="DISABLED"
  code={`<Toggle checked={false} disabled label="DISABLED OFF" />
<Toggle checked={true} disabled label="DISABLED ON" />`}
>
  <ToggleDisabledDemo />
</ComponentPreview>

## Props

<PropsTable
  props={[
    { name: "checked", type: "boolean", default: "false", description: "Controlled checked state" },
    { name: "onChange", type: "(checked: boolean) => void", description: "Change handler — receives the new checked value" },
    { name: "label", type: "string", description: "Label text (displayed uppercase)" },
    { name: "color", type: '"orange" | "green" | "cyan"', default: '"orange"', description: "Color theme" },
    { name: "size", type: '"sm" | "md" | "lg"', default: '"md"', description: "Size preset" },
    { name: "disabled", type: "boolean", default: "false", description: "Disables interaction and dims the toggle" },
    { name: "className", type: "string", default: '""', description: "Additional CSS classes" },
  ]}
/>

<Callout type="info" title="CONTROLLED COMPONENT">
  Toggle is fully controlled — you must manage `checked` state externally and pass an `onChange` handler. The sliding thumb emits a glow effect matching the active color theme.
</Callout>
