import { Skeleton } from "@/components";
import { ComponentPreview, PropsTable, Callout } from "@/components/docs";

# Skeleton

Animated placeholder skeleton with a horizontal scanline shimmer effect. Supports text lines, rectangular blocks, and circular shapes for loading states.

```tsx
import { Skeleton } from "@mattloyed/eva-ui";
```

## Text Variant

<ComponentPreview
  title="TEXT"
  code={`import { Skeleton } from "@mattloyed/eva-ui";

<Skeleton variant="text" />`}
>
  <Skeleton variant="text" />
</ComponentPreview>

## Rectangular

<ComponentPreview
  title="RECTANGULAR"
  code={`<Skeleton variant="rectangular" width={300} height={120} />`}
>
  <Skeleton variant="rectangular" width={300} height={120} />
</ComponentPreview>

## Circular

<ComponentPreview
  title="CIRCULAR"
  code={`<Skeleton variant="circular" width={64} height={64} />`}
>
  <Skeleton variant="circular" width={64} height={64} />
</ComponentPreview>

## Multi-line Text

<ComponentPreview
  title="MULTI-LINE"
  code={`<Skeleton variant="text" lines={4} />`}
>
  <Skeleton variant="text" lines={4} />
</ComponentPreview>

## Custom Dimensions

<ComponentPreview
  title="CUSTOM SIZE"
  code={`<div className="flex items-center gap-4">
  <Skeleton variant="circular" width={48} height={48} />
  <div className="flex-1 space-y-2">
    <Skeleton variant="text" width="60%" />
    <Skeleton variant="text" width="80%" />
  </div>
</div>`}
>
  <div className="flex items-center gap-4">
    <Skeleton variant="circular" width={48} height={48} />
    <div className="flex-1 space-y-2">
      <Skeleton variant="text" width="60%" />
      <Skeleton variant="text" width="80%" />
    </div>
  </div>
</ComponentPreview>

## Props

<PropsTable
  props={[
    { name: "variant", type: '"text" | "rectangular" | "circular"', default: '"text"', description: "Shape variant" },
    { name: "width", type: "string | number", description: "Width as CSS value or pixel number" },
    { name: "height", type: "string | number", description: "Height as CSS value or pixel number" },
    { name: "lines", type: "number", default: "1", description: "Number of text lines (only for text variant). Last line renders at 70% width." },
    { name: "className", type: "string", default: '""', description: "Additional CSS classes" },
  ]}
/>

<Callout type="info" title="SCANLINE SHIMMER">
  The skeleton uses a custom `eva-scanline` animation with an orange-tinted gradient sweep, matching the NERV terminal aesthetic.
</Callout>
