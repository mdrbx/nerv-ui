import { TerminalDisplay } from "@/components";
import { ComponentPreview, PropsTable } from "@/components/docs";

# Terminal Display

Monospace terminal emulator with optional typewriter effect. Auto-scrolls to bottom. Shows a blinking cursor, optional line numbers, and a status bar.

```tsx
import { TerminalDisplay } from "@mdrbx/nerv-ui";
```

## Basic Usage

<ComponentPreview
  title="DEFAULT"
  code={`<TerminalDisplay
  lines={[
    "MAGI SYSTEM v2.11 — BOOT SEQUENCE",
    "> Initializing MELCHIOR...",
    "> Initializing BALTHASAR...",
    "> Initializing CASPER...",
    "> All systems nominal.",
    "> Awaiting operator input..."
  ]}
  color="green"
  title="TERMINAL — MAGI DIRECT"
/>`}
>
  <TerminalDisplay
    lines={[
      "MAGI SYSTEM v2.11 — BOOT SEQUENCE",
      "> Initializing MELCHIOR...",
      "> Initializing BALTHASAR...",
      "> Initializing CASPER...",
      "> All systems nominal.",
      "> Awaiting operator input...",
    ]}
    color="green"
    title="TERMINAL — MAGI DIRECT"
  />
</ComponentPreview>

## Color Variants

<ComponentPreview
  title="ORANGE"
  code={`<TerminalDisplay
  lines={["WARNING: Sync rate fluctuation detected", "EVA-01 neural link unstable"]}
  color="orange"
  title="ALERT LOG"
/>`}
>
  <TerminalDisplay
    lines={[
      "WARNING: Sync rate fluctuation detected",
      "EVA-01 neural link unstable",
    ]}
    color="orange"
    title="ALERT LOG"
  />
</ComponentPreview>

## With Typewriter Effect

<ComponentPreview
  title="TYPEWRITER"
  code={`<TerminalDisplay
  lines={["Connecting to MAGI...", "Authentication verified.", "Welcome, Commander."]}
  typewriter
  typeSpeed={20}
  lineDelay={90}
  color="cyan"
/>`}
>
  <TerminalDisplay
    lines={[
      "Connecting to MAGI...",
      "Authentication verified.",
      "Welcome, Commander.",
    ]}
    typewriter
    typeSpeed={20}
    lineDelay={90}
    color="cyan"
  />
</ComponentPreview>

Use `typeSpeed` and `lineDelay` together when a feed needs to feel more reactive, for example on bulletin or incident-log views. Lower values make the reveal faster, and changing the `lines` prop resets the typewriter sequence cleanly.

## With Line Numbers

<ComponentPreview
  title="LINE NUMBERS"
  code={`<TerminalDisplay
  lines={["import { MAGI } from 'nerv';", "", "const result = MAGI.evaluate();", "console.log(result.consensus);"]}
  showLineNumbers
  color="green"
  title="SOURCE"
/>`}
>
  <TerminalDisplay
    lines={[
      "import { MAGI } from 'nerv';",
      "",
      "const result = MAGI.evaluate();",
      "console.log(result.consensus);",
    ]}
    showLineNumbers
    color="green"
    title="SOURCE"
  />
</ComponentPreview>

## Props

<PropsTable
  props={[
    {
      name: "lines",
      type: "string[]",
      required: true,
      description: "Array of text lines to display",
    },
    {
      name: "typewriter",
      type: "boolean",
      default: "false",
      description: "Enable character-by-character typing animation",
    },
    {
      name: "typeSpeed",
      type: "number",
      default: "30",
      description: "Milliseconds per character in typewriter mode; lower is faster",
    },
    {
      name: "lineDelay",
      type: "number",
      default: "200",
      description: "Milliseconds between lines in typewriter mode; lower is faster",
    },
    {
      name: "color",
      type: '"green" | "orange" | "cyan" | "red"',
      default: '"green"',
      description: "Terminal text color",
    },
    {
      name: "showCursor",
      type: "boolean",
      default: "true",
      description: "Show blinking block cursor",
    },
    { name: "title", type: "string", description: "Title bar text" },
    {
      name: "maxHeight",
      type: "string",
      default: '"400px"',
      description: "Max container height",
    },
    {
      name: "showLineNumbers",
      type: "boolean",
      default: "false",
      description: "Show line number gutter",
    },
    {
      name: "prompt",
      type: "string",
      description: "Prompt prefix for each line",
    },
    {
      name: "className",
      type: "string",
      default: '""',
      description: "Additional CSS classes",
    },
  ]}
/>
