import { Textarea } from "@/components";
import { ComponentPreview, PropsTable, Callout } from "@/components/docs";

# Textarea

Multi-line text input with animated bracket indicators on focus and monospaced EVA styling. Supports label, error, and hint text.

```tsx
import { Textarea } from "@mattloyed/eva-ui";
```

## Basic

<ComponentPreview
  title="BASIC"
  code={`<Textarea label="MISSION REPORT" placeholder="Enter report data..." />`}
>
  <Textarea label="MISSION REPORT" placeholder="Enter report data..." />
</ComponentPreview>

## With Error

<ComponentPreview
  title="ERROR"
  code={`<Textarea
  label="PILOT LOG"
  error="TRANSMISSION CORRUPTED — RETRY"
  defaultValue="Partial data received..."
/>`}
>
  <Textarea
    label="PILOT LOG"
    error="TRANSMISSION CORRUPTED — RETRY"
    defaultValue="Partial data received..."
  />
</ComponentPreview>

## Colors

<ComponentPreview
  title="COLORS"
  code={`<div className="flex flex-col gap-4">
  <Textarea color="orange" label="ORANGE" placeholder="Orange theme..." />
  <Textarea color="green" label="GREEN" placeholder="Green theme..." />
  <Textarea color="cyan" label="CYAN" placeholder="Cyan theme..." />
</div>`}
>
  <div className="flex flex-col gap-4">
    <Textarea color="orange" label="ORANGE" placeholder="Orange theme..." />
    <Textarea color="green" label="GREEN" placeholder="Green theme..." />
    <Textarea color="cyan" label="CYAN" placeholder="Cyan theme..." />
  </div>
</ComponentPreview>

## Props

<PropsTable
  props={[
    { name: "label", type: "string", description: "Label text (displayed uppercase with // prefix)" },
    { name: "color", type: '"orange" | "green" | "cyan"', default: '"orange"', description: "Color theme" },
    { name: "error", type: "string", description: "Error message — shown below the textarea in red" },
    { name: "hint", type: "string", description: "Helper text — shown below the textarea when no error" },
    { name: "wrapperClassName", type: "string", default: '""', description: "Additional CSS classes for the outer wrapper" },
    { name: "className", type: "string", default: '""', description: "Additional CSS classes for the textarea element" },
  ]}
/>

<Callout type="info" title="BRACKET FOCUS INDICATOR">
  When the textarea receives focus, animated `[` and `]` brackets slide in from either side, providing a clear visual indicator matching the NERV terminal aesthetic.
</Callout>
