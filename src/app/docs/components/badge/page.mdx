import { Badge } from "@/components";
import { ComponentPreview, PropsTable, Callout } from "@/components/docs";

# Badge

Uppercase tactical badge with corner accent marks and configurable variant colors. Used for status labels, tags, and categorization throughout the interface.

```tsx
import { Badge } from "@mattloyed/eva-ui";
```

## Variants

<ComponentPreview
  title="DEFAULT"
  code={`import { Badge } from "@mattloyed/eva-ui";

<Badge label="STANDBY" />`}
>
  <Badge label="STANDBY" />
</ComponentPreview>

<ComponentPreview
  title="SUCCESS"
  code={`<Badge label="OPERATIONAL" variant="success" />`}
>
  <Badge label="OPERATIONAL" variant="success" />
</ComponentPreview>

<ComponentPreview
  title="WARNING"
  code={`<Badge label="CAUTION" variant="warning" />`}
>
  <Badge label="CAUTION" variant="warning" />
</ComponentPreview>

<ComponentPreview
  title="DANGER"
  code={`<Badge label="CRITICAL" variant="danger" />`}
>
  <Badge label="CRITICAL" variant="danger" />
</ComponentPreview>

<ComponentPreview
  title="INFO"
  code={`<Badge label="MAGI ONLINE" variant="info" />`}
>
  <Badge label="MAGI ONLINE" variant="info" />
</ComponentPreview>

## Sizes

<ComponentPreview
  title="SIZES"
  code={`<div className="flex items-center gap-3">
  <Badge label="SMALL" size="sm" />
  <Badge label="MEDIUM" size="md" />
  <Badge label="LARGE" size="lg" />
</div>`}
>
  <div className="flex items-center gap-3">
    <Badge label="SMALL" size="sm" />
    <Badge label="MEDIUM" size="md" />
    <Badge label="LARGE" size="lg" />
  </div>
</ComponentPreview>

## Removable

<ComponentPreview
  title="REMOVABLE"
  code={`<div className="flex items-center gap-3">
  <Badge label="UNIT-00" removable onRemove={() => {}} />
  <Badge label="UNIT-01" variant="success" removable onRemove={() => {}} />
  <Badge label="UNIT-02" variant="danger" removable onRemove={() => {}} />
</div>`}
>
  <div className="flex items-center gap-3">
    <Badge label="UNIT-00" removable />
    <Badge label="UNIT-01" variant="success" removable />
    <Badge label="UNIT-02" variant="danger" removable />
  </div>
</ComponentPreview>

## Props

<PropsTable
  props={[
    { name: "label", type: "string", description: "Badge text (rendered uppercase)", required: true },
    { name: "variant", type: '"default" | "success" | "warning" | "danger" | "info"', default: '"default"', description: "Color variant" },
    { name: "size", type: '"sm" | "md" | "lg"', default: '"md"', description: "Size preset controlling padding and font size" },
    { name: "removable", type: "boolean", default: "false", description: "Show an X remove button" },
    { name: "onRemove", type: "() => void", description: "Callback when the remove button is clicked" },
    { name: "className", type: "string", default: '""', description: "Additional CSS classes" },
  ]}
/>

<Callout type="info" title="CORNER ACCENTS">
  Each badge features L-bracket corner marks that match the variant border color, giving it the characteristic NERV terminal aesthetic.
</Callout>
