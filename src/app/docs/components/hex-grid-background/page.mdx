import Link from "next/link";
import { HexGridBackground } from "@/components";
import { ComponentPreview, PropsTable, Callout } from "@/components/docs";

# Hex Grid Background

SVG-based honeycomb hexagon grid pattern. Positioned absolute to fill its parent container. This is a generic sci-fi surface texture, not a specific Evangelion control-room motif.

```tsx
import { HexGridBackground } from "@mdrbx/nerv-ui";
```

<Callout type="warning" title="GENERIC TEXTURE">
  `HexGridBackground` is useful for neutral futuristic surfaces, but if you want
  a backdrop that reads more directly like NERV / MAGI monitor framing, use{" "}
  <Link href="/docs/components/monitor-overlay">Monitor Overlay</Link>.
</Callout>

## Basic Usage

<ComponentPreview
  title="DEFAULT"
  code={`<div className="relative h-48 border border-nerv-mid-gray">
  <HexGridBackground color="#FF9900" opacity={0.15} />
  <div className="relative z-10 flex items-center justify-center h-full">
    <span className="text-nerv-orange font-mono text-sm">HONEYCOMB SURFACE</span>
  </div>
</div>`}
>
  <div className="relative h-48 border border-nerv-mid-gray">
    <HexGridBackground color="#FF9900" opacity={0.15} />
    <div className="relative z-10 flex items-center justify-center h-full">
      <span className="text-nerv-orange font-mono text-sm">HONEYCOMB SURFACE</span>
    </div>
  </div>
</ComponentPreview>

## Color Variants

<ComponentPreview
  title="COLORS"
  code={`<div className="grid grid-cols-3 gap-4">
  <div className="relative h-32 border border-nerv-mid-gray">
    <HexGridBackground color="#00FF00" opacity={0.12} />
    <div className="relative z-10 flex items-center justify-center h-full">
      <span className="text-nerv-green font-mono text-xs">GREEN</span>
    </div>
  </div>
  <div className="relative h-32 border border-nerv-mid-gray">
    <HexGridBackground color="#00FFFF" opacity={0.12} />
    <div className="relative z-10 flex items-center justify-center h-full">
      <span className="text-nerv-cyan font-mono text-xs">CYAN</span>
    </div>
  </div>
  <div className="relative h-32 border border-nerv-mid-gray">
    <HexGridBackground color="#FF0000" opacity={0.12} />
    <div className="relative z-10 flex items-center justify-center h-full">
      <span className="text-nerv-red font-mono text-xs">RED</span>
    </div>
  </div>
</div>`}
>
  <div className="grid grid-cols-3 gap-4">
    <div className="relative h-32 border border-nerv-mid-gray">
      <HexGridBackground color="#00FF00" opacity={0.12} />
      <div className="relative z-10 flex items-center justify-center h-full">
        <span className="text-nerv-green font-mono text-xs">GREEN</span>
      </div>
    </div>
    <div className="relative h-32 border border-nerv-mid-gray">
      <HexGridBackground color="#00FFFF" opacity={0.12} />
      <div className="relative z-10 flex items-center justify-center h-full">
        <span className="text-nerv-cyan font-mono text-xs">CYAN</span>
      </div>
    </div>
    <div className="relative h-32 border border-nerv-mid-gray">
      <HexGridBackground color="#FF0000" opacity={0.12} />
      <div className="relative z-10 flex items-center justify-center h-full">
        <span className="text-nerv-red font-mono text-xs">RED</span>
      </div>
    </div>
  </div>
</ComponentPreview>

## Props

<PropsTable
  props={[
    {
      name: "color",
      type: "string",
      default: '"#FF9900"',
      description: "Hex color for the grid strokes",
    },
    {
      name: "opacity",
      type: "number",
      default: "0.08",
      description: "Grid opacity (0-1)",
    },
    {
      name: "hexSize",
      type: "number",
      default: "30",
      description: "Individual hexagon size in pixels",
    },
    {
      name: "animated",
      type: "boolean",
      default: "true",
      description: "Enable pulse animation",
    },
    {
      name: "className",
      type: "string",
      default: '""',
      description: "Additional CSS classes",
    },
  ]}
/>
